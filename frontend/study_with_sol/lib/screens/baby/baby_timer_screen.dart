import 'package:flutter/material.dart';
// 타이머 만들기
class BabyTimer extends StatelessWidget {
  const BabyTimer({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
