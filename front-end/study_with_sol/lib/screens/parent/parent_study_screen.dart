// calender 라이브러리 사용해서 구현

import 'package:flutter/material.dart';

class ParentStudy extends StatelessWidget {
  const ParentStudy({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
